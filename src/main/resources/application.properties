spring.application.name=demo

# Logging
logging.file.name=application.log
logging.level.com.example.demo=INFO

# Oracle 18c XE Configuration
#spring.datasource.url=jdbc:oracle:thin:@//localhost:1521/XE 

#Раскомментировать для работы с существующей базой даннных
# Если не будет урла приложение будет сохранять схемы, обновлять схемы в локальной памяти
spring.datasource.username=system
spring.datasource.password=snopatd
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver

# Connection pool
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.connection-timeout=30000

